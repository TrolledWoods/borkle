const alloc   = extern "borkle_lib/hello" "alloc" : fn i64 -> i64;
const dealloc = extern "borkle_lib/hello" "dealloc" : fn i64, i64;

const main = {
    let ptr = alloc(8);
    dealloc(ptr, 8);
    ptr
};
