const main = {
    let x = print_i64(42);
    let y = &x;
    let num = &42;
    *num
};

const z = print_i64(y);
const x = print_i64(900);
const y = print_i64(x);

const other = print_i64(main - 500);

const print_u8  = extern "borkle_lib/hello" "print_u8"  : extern fn u8 -> u8;
const print_i64 = extern "borkle_lib/hello" "print_i64" : extern fn i64 -> i64;
const callback  = extern "borkle_lib/hello" "callback"  : extern fn -> extern fn i64 -> i64;

const alloc   = extern "borkle_lib/hello" "alloc"   : extern fn i64 -> &u8;
const dealloc = extern "borkle_lib/hello" "dealloc" : extern fn &u8, i64;
