const B = {
    let function = fn (a: u32, b: u32) -> u32  a + b * 3;

    cast(&[&a, &1, &3]) : [] any T &T;

    anify((a, a_function));

    let a = 1;
    let b = a;

    let c = 9;
    let d = 0;
    defer a + d;

    // let thing = [a, 1, 3];

    c = b;
    d = c;

    (a + 6) : u8;
};


