const B = {

    let a1 = fn(a: fn(&rw u32) -> &rw  u32) -> &rw _  a(#0);
    let a2 = fn(a: fn(&w  u32) -> &!!  u32) -> &w  _ {
        let b = #0;
        a(&b);
        &b
    };

    // Mix the types up, an if would be cooler here but I don't have those yet...
    let c = a1;
    c = a2;

    let x = #0 : u32;
    let hi = fn(x: _) -> _  x;
    *other_thing(hi) = x;

};
