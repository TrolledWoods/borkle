library "intrinsics.bo";

#entry fn() -> _ { 'block
    let a = 9 : u64;

    defer print("Deferred first\n");
    {
        {
            defer print("Deferred second\n");
        };
        defer print("Deferred third\n");
        break 'block 5 + 4 : typeof (a + 1);
    };

    break 'block 35;

    2 + 6
};

const print = fn(string: [] u8) {
    i_stdout_write(string);
    i_stdout_flush();
};
