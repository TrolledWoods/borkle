library "fmt.bo";
library "mem.bo";
// library "intrinsics.bo";

const VecU8 = type VecU8 {
    capacity : usize;
    buf : [] u8;

    #alias len = buf.len;
    #alias ptr = buf.ptr;
};

#entry fn() -> u64 {
    print("Hello, world!\n");

    let buffer = alloc(8);

    dealloc(buffer);

    print("Got past allocation\n");

    0
};
