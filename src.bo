const B = {

    let a1 = fn(a: fn(&_) -> &!! _) -> &_  { #0 };
    let a2 = fn(a: fn(&_) -> &_) -> &_   { a(#0) };

    let c = if #0 a1 else a2;

    let x = #0 : u32;
    let hi = fn(x: _) -> _ { x };
    *c(hi) = x;

};
