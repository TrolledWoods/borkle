import "tests/main.bo";
library "iter.bo";
library "intrinsics.bo";
library "vec.bo";

#entry fn() {
    let values = [1 : u32, 2, 3];
    for values print("%: %\n", (i, base(v, 3)));
};

// TODO:
// - Make [ptr + usize -> _] also infer to [ptr + usize -> ptr] (maybe similar for other operators too?)
