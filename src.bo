library "intrinsics.bo";

#entry fn() -> u64 {
    let var = bool_to_string(1 == (1 : u8));

    print(var);
    print("\n");

    5
};

const bool_to_string=fn(v:_)->_ if v"true"else"false";

const print = fn(string: [] u8) {
    i_stdout_write(string);
    i_stdout_flush();
};
