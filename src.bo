const B = {
    let c = #0 : &const _;
    c : &const _;

    c = #0 : &u32;

    // let c_copy = &c;
    // c_copy : &const u32;

    let ptr_to_c = &c : &let &u32;
    *ptr_to_c = #0 : &u32;

    let last_ptr = ptr_to_c;
    // let read = *last_ptr;
    // *read = #0;
};


