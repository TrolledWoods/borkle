// library "base.bo";
// import "tests/main.bo";

const CreateZeroed.(N) = fn(v: [N] u32) -> [N] u32  v;

#entry fn() -> u64 { 'func
    CreateZeroed.(_)(#0) : [39] _;
    
    5
};




    // This crashes for now
    // let stack_array = #0 : [
    //     count_matches.(u8)("Hello ,,,, ,, , , ,", fn(v) *v == COMMA)
    // ] u64;
