const B = {

    let a1 = fn(a: fn(&_) -> &_) -> &_  { #0 };
    let a2 = fn(a: fn(&_) -> &_) -> &_  { a(#0) };

    a1 : fn(fn(&rw _)->&_)->&rw _;

    let c = #0;
    let c_write = &c;

    let condition = #0;
    if condition {
        *c_write = a1;
    } else {
        *c_write = a2;
    };

    let x = #0 : u32;
    let hi = fn(x: _) -> _ { x };
    *c(hi) = x;

};
