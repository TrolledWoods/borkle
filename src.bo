library "base.bo";
// import "tests/main.bo";

const Buffer[T] = type [] T;

const BufferU8 = Buffer[u8];

#entry fn() -> u64 { 'func
    4
};


// {
//     let buffer_type = if (1 : u8) > 0  type [] u8 else type u8;
// 
//     buffer_type
// };
