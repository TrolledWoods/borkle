const B = {

    let c = #0 : &u32;
    let thing = *c;
    let ptr_to_c = &c : & &rw u32;
    *ptr_to_c = #0 : &u32;

    // We probably cannot know if we know the type of something completely. Unless,
    // we already know exactly what value it has to be.

    // let read = *last_ptr;
    // *read = #0;
};


