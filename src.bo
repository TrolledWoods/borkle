library "mem.bo";
library "fmt.bo";

const assign = fn(assignment: &let u32) {
    let bad = &let *assignment;
    *assignment = 42;
};

const main = fn() -> u64 {
    let x = #uninit : u32;
    assign(&let x);

    0
};

#entry main;






// We add some redundant libraries down here so that we can make sure they
// compile.
library "string.bo";
library "vec_u8.bo";
