library "fmt.bo";

#entry fn() -> u64 {
    print("%\n", &[fmt_u64(factorial(3))]);

    0
};

const factorial = fn(num: u64) -> u64 { 'function
    if num == 0 break 'function 1;

    factorial(num - 1) * num
};
