library "base.bo";
import "tests/main.bo";

#entry fn() -> u64 { 'func
    let thing = &[1, 2, 3] : [] _;
    *thing.ptr
};









    // This crashes for now
    // let stack_array = #0 : [
    //     count_matches.(u8)("Hello ,,,, ,, , , ,", fn(v) *v == COMMA)
    // ] u64;

