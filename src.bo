library "iter.bo";
library "vec.bo";

const print_tuple.(A, B) = fn((a: A, b: B)) {
    print_u64(cast a);
    print(", ");
    print_u64(cast b);
    print("\n");
};

{

    /*let vec = get_line();
    defer vec_free(vec);

    for vec_as_buffer(&vec) {
        print_u64(cast v);
        print("\n");
    };*/

    /*let array = [1, 2, 3, 4, 5];

    for cast &array : [] u64 {
        print_u64(v);
        print("\n");
    };*/

    let tuple = (1 : u64, 2 : u64);

    print_tuple(tuple);
};
