// library "base.bo";
// import "tests/main.bo";

// This crashes for now
//
// let stack_array = #0 : [
//     count_matches.(u8)("Hello ,,,, ,, , , ,", fn(v) *v == COMMA)
// ] u64;

const OtherThing.(T) = fn(x: T) -> T  x;

const Thing.(T) = fn(v: T) -> T {
    // OtherThing.(type fn(T) -> T)(fn(x: T) -> T  x)(v)
    OtherThing(fn(x: T) -> T  x)(v)
};

