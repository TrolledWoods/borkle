library "fmt.bo";

const main = fn() -> u64 {
    let buffer = &[5, 1, 2, 3, 6, 8, 7, 9, 0, 4] : [] i64;

    let index = for buffer {
        if *it == 6  break bit_cast it - bit_cast buffer.ptr : i64;
    } else -1 : i64;

    print("The index is %\n", &[fmt_i64(index)]);

    5
};
