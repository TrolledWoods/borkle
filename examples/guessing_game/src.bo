library "mem.bo";
library "io.bo";

const main = fn() -> u64 {
    put_string("Enter the number the other person should guess(don't let them see it): \n");

    let input = read_input();
    defer dealloc(input);

    let answer = string_to_int(input);

    let i = 1000 : i64;
    while i > 0 {
        put_string("\n");
        i = i - 1;
    };

    let guess = 0 : u64;
    while guess != answer {
        put_string("Take a guess!\n");
        let input = read_input();
        defer dealloc(input);

        guess = string_to_int(input); 

        if guess < answer {
            put_string("Too small!\n");
        } else if guess > answer  {
            put_string("Too big!\n");
        };
    };

    put_string("That is correct! :D\n");

    0
};
