#no_base:

type #builtin: CallingConvention = enum u8 {
    Borkle = 0;
    C = 1;
};

type #builtin: Target = enum u32 {
    Compiler = 0x01;
    Native   = 0x02;

    All      = cast (-1 : i32);
};

type #builtin: CString = &u8;
