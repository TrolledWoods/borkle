library "fmt.bo";

const slice[T] = fn(slice: [] T, start: usize, end: usize) -> [] T {
    let sliced = #uninit : [] T;
    sliced.ptr = slice.ptr + start;
    sliced.len = end - start;
    sliced
};

const slice_equals[T] = fn(a: [] T, b: [] T) -> bool { 'function
    if a.len != b.len {
        break 'function 0 > 1;
    };
    for a  if *(b.ptr + _iters) != *_it {
        break 'function 0 > 1;
    };
    1 > 0
};
